<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Home', users_path %><span class="sr-only">(current)</span></li>
        <li><%= link_to 'Beers', beers_path %><span class="sr-only">(current)</span></li>
        <% if current_user %>
              <li>Logged in as: <%= current_user.first %><span class="sr-only">(current)</span></li>
              <li> <%= link_to 'Log Out', logout_path, method: :delete %></li><span class="sr-only">(current)</span></li>
          <% else %>
          <li><%= link_to 'Log In', login_path %><span class="sr-only">(current)</span></li>
          <li><%= link_to 'Sign Up', new_user_path %><span class="sr-only">(current)</span></li>
          <% end %>
        <li class="dropdown">
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>